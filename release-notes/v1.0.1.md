# WorldAreaReset 1.0.1

## English

- Fix startup migration for known v1-v25 configurations after the public schema marker reset to `config-version: 1`.
- Keep refusing unknown future schemas rather than overwriting them.

## 中文

- 修复公开配置标记重置为 `config-version: 1` 后，已知 v1-v25 配置的启动迁移。
- 继续拒绝未知的未来配置格式，不会覆盖原文件。
