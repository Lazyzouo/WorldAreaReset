# WorldAreaReset 1.0.0

## English

- Reissue the first public `WorldAreaReset-1.0.0.jar` with the English default configuration and bundled Chinese template.
- Start the public configuration schema at `config-version: 1`.
- Preserve Kitloader-compatible message formatting, safe migration, atomic backups, verified updates, and the 50 MiB download limit.

## 中文

- 重新发布首个公开包 `WorldAreaReset-1.0.0.jar`，默认使用英文配置并内置中文模板。
- 将公开配置结构从 `config-version: 1` 开始。
- 保留与 Kitloader 兼容的消息格式、安全迁移、原子备份、更新校验和 50 MiB 下载上限。
